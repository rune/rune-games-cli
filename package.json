{
  "name": "rune-games-cli",
  "version": "1.0.0",
  "type": "module",
  "repository": "git@github.com:rune/rune-games-cli.git",
  "author": "Helios1138 <revan.den@gmail.com>",
  "license": "MIT",
  "bin": {
    "rune": "./dist/index.js"
  },
  "engines": {
    "node": ">=14.16"
  },
  "scripts": {
    "cli": "ts-node --esm src",
    "build": "rm -rf dist && tsc && chmod +x dist/index.js",
    "buildWrapper": "rm -rf src/wrapper && cd ../TinCan && CLI=1 yarn web:build && cp -r app/web/build ../rune-games-cli/src/wrapper && cd ../rune-games-cli && git add --all src/wrapper",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.25",
    "@types/react": "^18.0.5",
    "express": "^4.18.0",
    "get-port": "^6.1.2",
    "ink": "^3.2.0",
    "meow": "^10.1.2",
    "prettier": "^2.6.2",
    "react": "^18.0.0",
    "rune-games-sdk": "^1.4.8",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
